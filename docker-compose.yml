name: fauzi-portfolio

services:
  api:
    image: fauzia24/portofolio-api:v1.0.0
    container_name: portfolio-api
    restart: unless-stopped
    networks:
      service_service_network:
        aliases:
          - portfolio-api
          - api
      database_database_network:
      project_network:
        aliases:
          - portfolio-api
          - api
    environment:
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL is required}
      DATABASE_POOL_MAX: ${DATABASE_POOL_MAX:-5}
      PORT: 3001
      LOG_LEVEL: ${LOG_LEVEL:-info}
      TRUST_PROXY: "true"
      API_BODY_LIMIT_BYTES: ${API_BODY_LIMIT_BYTES:-6291456}
      CORS_ORIGIN: ${PUBLIC_ORIGIN:?PUBLIC_ORIGIN is required}
      ADMIN_TOKEN: ${ADMIN_TOKEN:?ADMIN_TOKEN is required}
      ADMIN_EMAIL: ${ADMIN_EMAIL:?ADMIN_EMAIL is required}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?ADMIN_PASSWORD is required}
      S3_ENDPOINT: ${S3_ENDPOINT:?S3_ENDPOINT is required}
      S3_REGION: ${S3_REGION:-auto}
      S3_BUCKET: ${S3_BUCKET:?S3_BUCKET is required}
      S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID:?S3_ACCESS_KEY_ID is required}
      S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY:?S3_SECRET_ACCESS_KEY is required}
      S3_FORCE_PATH_STYLE: "true"
      S3_PUBLIC_URL: ${PUBLIC_ORIGIN}/s3/${S3_BUCKET}
    command: >
      sh -c "pnpm --filter @portfolio/api db:migrate &&
             until pnpm --filter @portfolio/api storage:setup; do sleep 2; done &&
             pnpm --filter @portfolio/api db:seed &&
             pnpm --filter @portfolio/api start"

  web:
    image: fauzia24/portofolio-web:v1.0.0
    container_name: portfolio-web
    restart: unless-stopped
    networks:
      service_service_network:
        aliases:
          - portfolio-api
          - api
      database_database_network:
      project_network:
        aliases:
          - portfolio-api
          - api
networks:
  service_service_network:
    external: true
  database_database_network:
    external: true
  project_network:
    external: true
